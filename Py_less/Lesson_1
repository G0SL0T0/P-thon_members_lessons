import random

def get_event_numbers(numbers):
    event_numbers = []
    for num in numbers:
        if num % 2 == 0:
            event_numbers.append(num)
    return event_numbers

numbers = [random.randint(0,100) for _ in range(10)]
print(get_event_numbers(numbers))